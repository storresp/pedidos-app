frontend:
  replicaCount: 1
  image:
    tag: "dev"
  resources:
    requests:
      cpu: 100m
      memory: 128Mi

backend:
  replicaCount: 1
  image:
    tag: "dev"
  resources:
    requests:
      cpu: 100m
      memory: 128Mi

postgresql:
  auth:
    username: "postgres"
    password: "prodpassword"
    database: "pedidos_prod"

ingress:
  hosts:
    - host: "prod.pedido-app.local"
    - host: "dev.pedido-app.local"
